---
- name: Surface Book playbook
  hosts: localhost

  tasks:
    - name: Install main dnf packages
      become: yes
      ansible.builtin.dnf5:
        name:
          - bat
          - fd-find
          - fish
          - gcc
          - git
          - kitty-terminfo
          - ripgrep
          - zsh
        state: present

    # community.general.dnf_config_manager isn't ready for dnf 5 yet
    - name: Add surface-linux dnf repo
      become: yes
      ansible.builtin.command:
        argv:
          - /usr/bin/dnf
          - config-manager
          - addrepo
          - --from-repofile=https://pkg.surfacelinux.com/fedora/linux-surface.repo
        creates: /etc/yum.repos.d/linux-surface.repo

    - name: Remove unwanted RPMs
      become: yes
      ansible.builtin.dnf5:
        name:
          - elisa-player
          - firefox # prefer flatpak
          - kcalc
          - kmahjongg
          - kmines
          - kolourpaint
          - kpat
          - neochat
          - okular
          - skanpage
        state: absent

    - name: Set user's shell to fish
      become: yes
      ansible.builtin.user:
        name: '{{ ansible_user_id }}'
        shell: /usr/bin/fish

    - name: Install flatpaks from flathub
      community.general.flatpak:
        name:
          - app.drey.Warp
          - com.boxy_svg.BoxySVG
          - com.github.flxzt.rnote
          - com.github.xournalpp.xournalpp
          - com.ticktick.TickTick
          - com.usebottles.bottles
          - com.yubico.yubioath
          - io.ente.auth
          - io.github.mhogomchungu.sirikali
          - io.github.mpobaschnig.Vaults
          - org.fkoehler.KTailctl
          - org.inkscape.Inkscape
          - org.kde.arianna
          - org.kde.elisa
          - org.kde.filelight
          - org.kde.index
          - org.kde.kalk
          - org.kde.kcalc
          - org.kde.kclock
          - org.kde.kmahjongg
          - org.kde.kmines
          - org.kde.koko
          - org.kde.kolourpaint
          - org.kde.kpat
          - org.kde.krita
          - org.kde.kweather
          - org.kde.okular
          - org.kde.optiimage
          - org.kde.pix
          - org.kde.skanpage
          - org.kde.tokodon
          - org.mozilla.firefox
          - org.videolan.VLC
          - xyz.armcord.ArmCord

    - name: Enable sshd socket activation
      become: yes
      ansible.builtin.systemd_service:
        name: sshd.socket
        enabled: yes
        state: started
