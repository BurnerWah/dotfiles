# chezmoi:template:left-delimiter="# {{" right-delimiter=}}
# {{ if and (lookPath "ss") (lookPath "grc") -}}
function ss -d "Utility to investigate sockets" --wraps=ss
    if isatty stdout; and command -q grc
        command grc ss $argv
    else
        command ss $argv
    end
end
# {{ end -}}
