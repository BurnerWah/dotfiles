# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: esbuild
description: An extremely fast JavaScript bundler
parsing: disabled
completion:
  positionalany: ['$carapace.bridge.Inshellisense([esbuild])']
