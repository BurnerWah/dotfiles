# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: mas
description: A simple command line interface for the Mac App Store
parsing: disabled
completion:
  positionalany: ['$carapace.bridge.Inshellisense([mas])']
