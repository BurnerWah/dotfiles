# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: aws-nuke
description: remove everything from an aws account
parsing: disabled
completion:
  positionalany: ['$carapace.bridge.Urfavecli([aws-nuke])']
