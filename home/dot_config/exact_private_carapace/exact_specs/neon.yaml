# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: neon
description: Neon CLI tool
parsing: disabled
completion:
  positionalany: ['$carapace.bridge.CarapaceBin([neonctl])']
