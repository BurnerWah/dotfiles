# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: notation
description: CLI tool to sign and verify OCI artifacts and container images
parsing: disabled
completion:
  positionalany: ['$carapace.bridge.Cobra([notation])']
