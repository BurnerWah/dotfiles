# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: networkQuality
description: Measure the different aspects of network quality
parsing: disabled
completion:
  positionalany: ['$carapace.bridge.Inshellisense([networkQuality])']
