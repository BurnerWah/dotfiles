# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: mocha
description: simple, flexible, fun test framework
parsing: disabled
completion:
  positionalany: ['$carapace.bridge.Yargs([mocha])']
