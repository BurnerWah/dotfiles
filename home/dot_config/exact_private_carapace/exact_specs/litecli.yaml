# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: litecli
description: A SQLite terminal client with auto-completion and syntax highlighting
parsing: disabled
completion:
  positionalany: ['$carapace.bridge.Click([litecli])']
