# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: xcrun
description: Run or locate development tools and properties from xcode
parsing: disabled
completion:
  positionalany: ['$carapace.bridge.Inshellisense([xcrun])']
