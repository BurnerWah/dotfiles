# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: kind
description: Kubernetes in Docker
parsing: disabled
completion:
  positionalany: ['$carapace.bridge.Cobra([kind])']
