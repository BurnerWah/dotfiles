# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: pipenv
description: Python dependency management tool
parsing: disabled
completion:
  positionalany: ['$carapace.bridge.Click([pipenv])']
