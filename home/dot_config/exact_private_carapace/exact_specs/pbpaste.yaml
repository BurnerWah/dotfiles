name: pbpaste
description: paste standard input to standard output
flags:
  -help: display help information
  -pboard=: specify pasteboard to copy to or paste from
  -Prefer=: specify type of data to prefer when pasting
completion:
  flag:
    pboard: [pasteboard, general, ruler, find, font]
    Prefer:
      - "txt\tplain text"
      - "rtf\trich text"
      - "ps\tencapsulated PostScript"
