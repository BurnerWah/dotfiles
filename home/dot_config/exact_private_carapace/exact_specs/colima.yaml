# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: colima
description: Container runtimes on MacOS (and Linux) with minimal setup
parsing: disabled
completion:
  positionalany: ['$carapace.bridge.Cobra([colima])']
