# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: iredis
description: Terminal Client for Redis with AutoCompletion and Syntax Highlighting
parsing: disabled
completion:
  positionalany: ['$carapace.bridge.Click([iredis])']
