# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: skopeo
description: Work with remote images registries
parsing: disabled
completion:
  positionalany: ['$carapace.bridge.Cobra([skopeo])']
