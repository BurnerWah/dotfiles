# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: fish-lsp
description: LSP implementation for the fish shell language
parsing: disabled
completion:
  positionalany: ['$carapace.bridge.Fish([fish-lsp])']
