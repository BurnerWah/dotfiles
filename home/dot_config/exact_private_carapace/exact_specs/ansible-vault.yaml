# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: ansible-vault
description: encryption/decryption utility for Ansible data files
parsing: disabled
completion:
  positionalany: ['$carapace.bridge.Argcomplete([ansible-vault])']
