# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: orbctl
description: Manage manage OrbStack and its machines
parsing: disabled
completion:
  positionalany: ['$carapace.bridge.Cobra([orbctl])']
