# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: cookiecutter
description: Utility that creates projects from templates
parsing: disabled
completion:
  positionalany: ['$carapace.bridge.Click([cookiecutter])']
