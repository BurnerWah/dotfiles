# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: whalebrew
description: Install Docker images as native commands
parsing: disabled
completion:
  positionalany: ['$carapace.bridge.Cobra([whalebrew])']
