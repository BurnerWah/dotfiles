# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: buf
description: New way of working with Protocol Buffers
parsing: disabled
completion:
  positionalany: ['$carapace.bridge.Cobra([buf])']
