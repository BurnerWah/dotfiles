# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: cue
description: Validate and define text-based and dynamic configuration
parsing: disabled
completion:
  positionalany: ['$carapace.bridge.Cobra([cue])']
