# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: carapace-bridge
description: A multi-shell completion bridge
parsing: disabled
completion:
  positionalany: ['$carapace.bridge.Carapace([carapace-bridge])']
