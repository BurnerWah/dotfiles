# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: dockerfmt
description: Dockerfile format and parser
parsing: disabled
completion:
  positionalany: ['$carapace.bridge.Cobra([dockerfmt])']
