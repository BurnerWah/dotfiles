# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: webstorm
description: WebStorm CLI
parsing: disabled
completion:
  positionalany: ['$carapace.bridge.CarapaceBin([idea])']
