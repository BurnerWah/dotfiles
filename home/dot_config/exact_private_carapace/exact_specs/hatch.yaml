# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: hatch
description: Modern, extensible Python project management
parsing: disabled
completion:
  positionalany: ['$carapace.bridge.Click([hatch])']
