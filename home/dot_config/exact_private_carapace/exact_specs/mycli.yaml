# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: mycli
description: A MySQL terminal client with auto-completion and syntax highlighting
parsing: disabled
completion:
  positionalany: ['$carapace.bridge.Click([mycli])']
