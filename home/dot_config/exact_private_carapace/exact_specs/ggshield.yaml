# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: ggshield
description: Scanner for secrets and sensitive data in code
parsing: disabled
completion:
  positionalany: ['$carapace.bridge.Click([ggshield])']
