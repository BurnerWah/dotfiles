# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: conc
description: Run commands concurrently in npm scripts
parsing: disabled
completion:
  positionalany: ['$carapace.bridge.CarapaceBin([concurrently])']
