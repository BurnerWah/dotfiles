# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: argo
description: Get stuff done with container-native workflows for Kubernetes
parsing: disabled
completion:
  positionalany: ['$carapace.bridge.Cobra([argo])']
