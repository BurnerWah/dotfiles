# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: velero
description: Disaster recovery for Kubernetes resources and persistent volumes
parsing: disabled
completion:
  positionalany: ['$carapace.bridge.Cobra([velero])']
