# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: rich
description: Rich text and formatting in the terminal
parsing: disabled
completion:
  positionalany: ['$carapace.bridge.Click([rich])']
