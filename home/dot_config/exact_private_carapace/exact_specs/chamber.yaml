# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: chamber
description: CLI for managing secrets through AWS SSM Parameter Store
parsing: disabled
completion:
  positionalany: ['$carapace.bridge.Cobra([chamber])']
