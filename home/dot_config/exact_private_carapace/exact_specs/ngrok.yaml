# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: ngrok
description: tunnel local ports to public URLs and inspect traffic
parsing: disabled
completion:
  positionalany: ['$carapace.bridge.Cobra([ngrok])']
