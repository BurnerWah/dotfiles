# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: commitlint
description: Lint your commit messages
parsing: disabled
completion:
  positionalany: ['$carapace.bridge.Yargs([commitlint])']
