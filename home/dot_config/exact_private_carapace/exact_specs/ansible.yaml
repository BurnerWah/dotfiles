# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: ansible
description: Define and run a single task 'playbook' against a set of hosts
parsing: disabled
completion:
  positionalany: ['$carapace.bridge.Argcomplete([ansible])']
