# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: sops
description: Editor of encrypted files
parsing: disabled
completion:
  positionalany: ['$carapace.bridge.UrfavecliV1([sops])']
