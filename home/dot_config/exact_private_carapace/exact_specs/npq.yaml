# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
# TODO create custom spec
name: npq
description: Audit npm packages before you install them
parsing: disabled
completion:
  positionalany: ['$carapace.bridge.Yargs([npq])']
