# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: gibo
description: Access GitHub's .gitignore boilerplates
parsing: disabled
completion:
  positionalany: ['$carapace.bridge.Cobra([gibo])']
