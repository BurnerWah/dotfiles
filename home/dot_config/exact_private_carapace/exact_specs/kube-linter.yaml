# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: kube-linter
description: Static analysis tool for Kubernetes YAML files and Helm charts
parsing: disabled
completion:
  positionalany: ['$carapace.bridge.Cobra([kube-linter])']
