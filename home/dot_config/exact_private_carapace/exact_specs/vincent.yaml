# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: vincent
description: terminal color scheme chooser
parsing: disabled
completion:
  positionalany: ['$carapace.bridge.Carapace([vincent])']
