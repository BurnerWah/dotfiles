# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: harlequin
description: Easy, fast, and beautiful database client for the terminal
parsing: disabled
completion:
  positionalany: ['$carapace.bridge.Click([harlequin])']
