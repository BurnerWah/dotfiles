# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: apptainer
description: Application container and unprivileged sandbox platform for Linux
parsing: disabled
completion:
  positionalany: ['$carapace.bridge.Cobra([apptainer])']
