# This is a base environment for the user to derive later configs from

# This might be implicitly set? Safer to just hardcode it tho
PATH=${PATH:-/usr/local/bin:/usr/local/sbin:/usr/bin:/usr/sbin}

# vim:ft=environment
