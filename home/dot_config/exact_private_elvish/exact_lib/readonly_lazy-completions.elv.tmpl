# Generated by chezmoi

{{- range $command, $completer := .settings.shell.elvish.completers }}
set edit:completion:arg-completer['{{ $command }}'] = {|@arg|
      set edit:completion:arg-completer['{{ $command }}'] = {|@arg|}
      eval ({{ $completer }} | slurp)
      $edit:completion:arg-completer['{{ $command }}'] $@arg
}
{{- end }}
