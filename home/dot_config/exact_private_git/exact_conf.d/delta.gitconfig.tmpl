# chezmoi:template:left-delimiter="# {{" right-delimiter=}}
# {{ if lookPath "delta" -}}
[core]
    pager = delta
[interactive]
    diffFilter = delta --color-only
[merge]
    conflictstyle = zdiff3
# {{ end -}}
