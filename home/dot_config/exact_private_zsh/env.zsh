# Shell sessions in caches
# SHELL_SESSION_DIR=${HOME}/Library/Caches/zsh_sessions
SHELL_SESSIONS_DISABLE=1

# for bkt
export BKT_SHELL_KEY=zsh

# Used in oh-my-zsh plugins
ZSH_CACHE_DIR="${XDG_CACHE_HOME}/oh-my-zsh"
