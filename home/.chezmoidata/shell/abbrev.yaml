settings:
  shell:
    common:
      abbrev:
        asdf: mise
        brwe: brew
        bvm: mise
        bwre: brew
        chruby: mise use ruby
        diff: delta
        dog: doggo
        exa: eza
        faas: faas-cli
        fades: uv run
        find: fd
        fly: flyctl
        fnm: mise use node
        frum: mise use ruby
        gdl: gallery-dl
        gobrew: mise use go
        goenv: mise use go
        gti: git
        gvm: mise use go
        jabba: mise use java
        ligma: lima
        nave: mise use node
        nc: ncat
        ndenv: mise use node
        neofetch: fastfetch
        nodebrew: mise use node
        nodenv: mise use node
        nvm: mise use node
        nvs: mise use node
        pipx: uv tool
        pyenv: uv
        rb: ruby
        rbenv: mise use ruby
        rpmq: rpm -q
        rtx: mise
        rye: uv
        tox: uv
        vf: uv
        vfox: mise
        yum: dnf

        l: ls
        sl: ls
        cls: clear
        dc: cd

        cat: bat
        batp: bat -p

        vi: nvim
        vim: nvim
        view: nvim -R
        ex: nvim -e

        grep: rg
        egrep: rg
        fgrep: rg -F
        zgrep: rg -z
        zegrep: rg -z
        zfgrep: rg -zF
        bzgrep: rg -z
        bzegrep: rg -z
        bzfgrep: rg -zF
        lzgrep: rg -z
        lzegrep: rg -z
        lzfgrep: rg -zF
        xzgrep: rg -z
        xzegrep: rg -z
        xzfgrep: rg -zF
        zstdgrep: rg -z

        ydl: yt-dlp
        ydlmp3: yt-dlp -x --audio-format mp3
        ydlopus: yt-dlp -x --audio-format opus
        youtube-dl: yt-dlp
    fish:
      abbrev:
        ?: if
        .: source
        al: la
        cmd: command
        export: set -x
        fn: function
        fns: functions
        fnsv: funcsave
        fun: function
        func: function
        h: help
        local: set -l
        loop: while true
        psrg: ps aux | rg
        which: whereis
        xopen: open
