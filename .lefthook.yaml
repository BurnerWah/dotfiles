pre-commit:
  parallel: true
  commands:
    prettier:
      run: npx prettier --write {staged_files}
      glob: '*.{css,yml,yaml,md,html,json,jsonc,code-snippets}'
      stage_fixed: true

    taplo:
      run: taplo format {staged_files}
      glob: '*.toml'
      stage_fixed: true

    ruff:
      run: ruff format {staged_files}
      glob: '*.py'
      stage_fixed: true
