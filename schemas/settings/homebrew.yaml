# yaml-language-server: $schema=https://json-schema.org/draft/2020-12/schema
title: homebrew config schema
description: homebrew config
type: object
properties:
  forbidden:
    type: object
    additionalProperties: false
    properties:
      cask:
        type: array
        items: { type: string }
      formula:
        type: array
        items: { type: string }
      linux:
        type: array
        items: { type: string }
      tap:
        type: array
        items: { type: string }
