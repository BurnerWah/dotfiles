# Powershell is slow.
# It's performance is in the same league as xonsh (which is written in python)
# and around orders of magnitude slower than bash/zsh/fish.
# Combined with the lack of completions for it, and it's not all that useful
# for carapace bridges.
